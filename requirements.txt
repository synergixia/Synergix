# ── Telegram Bot Framework ──
aiogram==3.13.0

# ── HTTP / Async ──
httpx>=0.27.0,<0.29.0
aiofiles>=23.2.1,<24.0.0
starlette>=0.40.0,<0.42.0
uvicorn>=0.30.0
sse-starlette>=2.0.0

# ── Retry / Resilience ──
tenacity>=8.0.0,<10.0.0

# ── Greenfield SDK oficial (desde GitHub) ──
greenfield-python-sdk @ git+https://github.com/bnb-chain/greenfield-python-sdk.git

# ── IA Local (Qwen2.5 GGUF) ──
torch==2.5.1
numpy>=1.26.0,<2.0.0
llama-cpp-python==0.3.6
sentence-transformers>=3.3.0,<4.0.0
faiss-cpu>=1.7.4,<1.9.0

# ── Scheduling / Cron ──
apscheduler>=3.10.0,<4.0.0

# ── Config / Idiomas / Utilidades ──
python-dotenv>=1.0.0
pydantic>=2.4.1,<2.9
langdetect>=1.0.9

# ── Web3 / BSC / Trading ──
web3>=6.0.0,<8.0.0

# ── Requerido por betterproto y compilación ──
setuptools
